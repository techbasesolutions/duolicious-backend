__pycache__
*.swo
*.swp

# Agent scratch — workflows / plans dumps. Never committed; tracked
# only locally for in-session reference.
docs/superpowers/

venv/

test/input
test/output

node_modules

# design handoff dump (do not commit)
Ahavah-Claude Design/

# Production secrets — never commit. Template lives at .env.production.template.
.env
.env.local
.env.production
.env.staging
