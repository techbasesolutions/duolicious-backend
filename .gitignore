__pycache__
*.swo
*.swp

# Agent scratch under docs/superpowers/ — exclude everything by default,
# but ALLOW the specs/ and plans/ subdirs which hold deliberate, committable
# design + implementation artifacts.
docs/superpowers/*
!docs/superpowers/specs/
!docs/superpowers/plans/
!docs/superpowers/handovers/

venv/

test/input
test/output

node_modules

# design handoff dump (do not commit)
Ahavah-Claude Design/

# Production secrets — never commit. Template lives at .env.production.template.
.env
.env.local
.env.production
.env.staging
